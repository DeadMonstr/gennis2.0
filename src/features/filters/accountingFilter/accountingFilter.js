import {Modal} from "shared/ui/modal";

export const AccountingFilter = ({setActive , active}) => {
    return (
        <div>
            <Modal setActive={setActive} active={active}>

            </Modal>
        </div>
    );
};

